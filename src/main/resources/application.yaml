spring:
  application:
    name: hotel_booking

  datasource:
    url: jdbc:postgresql://acela.proxy.rlwy.net:24427/railway?sslmode=require
    username: postgres
    password: ktVCMUIoTvtOrwqdKtZKnFFXupglQrEI

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: ${PORT:8080}